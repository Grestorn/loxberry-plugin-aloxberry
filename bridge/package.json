{
  "name": "@aloxberry/bridge",
  "version": "0.6.3",
  "description": "Stateless dispatch bridge for the Aloxberry plugin. Holds persistent WSS connections from each Loxberry plugin and routes HMAC-signed directives from the AWS Lambda. Bridge sees opaque payloads — end-to-end auth is between Lambda and plugin.",
  "private": true,
  "main": "src/index.js",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "ws": "^8.18.0"
  }
}
